import RegisterForm from '../../components/auth/RegisterForm';

function Register() {
  return (
    <RegisterForm
      onSubmit={() => {
        return;
      }}
    />
  );
}

export default Register;
